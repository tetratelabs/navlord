import React from 'react'

const infoTypes = () => {

    const infoTypeList = infoData.map(infotype=> {
        return(
            <div>
                infotype.
            </div>
        )
    })

    return ( 
    <div>

    </div> 
    );
}
 
export default infoTypes;